let m = new Map([["apple",500],["key",500]]);
console.log(m.get("apple"))
// to create empty Map
let m1 = new Map();
// to add new values to map we have to use set() method.

m1.set("Male",300);
m1.set("Female",500)

// to clear the map
console.log(m1.clear())
console.log(m1)
